# Copy to .env for local `npm start` (host → docker-compose Mongo on :27017)
# Container path: use `make local-app` (uses host.docker.internal instead of localhost)
MONGO_URI=mongodb://localhost:27017/solar-system?authSource=admin
MONGO_USERNAME=admin
MONGO_PASSWORD=password
NODE_ENV=development
